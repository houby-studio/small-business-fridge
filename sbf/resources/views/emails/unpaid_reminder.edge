<!DOCTYPE html>
<html>
<head><meta charset="utf-8"></head>
<body style="font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
  <h2 style="color: #FF9800;">{{ i18n.t('emails.unpaid_reminder_heading') }}</h2>
  <p>{{ i18n.t('emails.unpaid_reminder_intro', { buyerName, id: invoiceId, supplierName, amount: totalCost }) }}</p>
  @if(supplierIban)
  <p>{{ i18n.t('emails.unpaid_reminder_iban') }} <strong>{{ supplierIban }}</strong></p>
  @end
  <p>{{ i18n.t('emails.unpaid_reminder_note') }}</p>
  <p style="color: #666; font-size: 12px;">— {{ i18n.t('common.app_name') }}</p>
</body>
</html>
